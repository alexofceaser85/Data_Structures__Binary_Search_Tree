package enums;

/**
 * The enum that holds the node type to store in the files this enum allows for compatibility 
 * between the full NodeType enum (QUESTION/ANSWER) and the short form (Q/A) values for the files.
 * 
 * @author Alex DeCesare
 * @version 22-April-2021
 */

public enum FileNodeType {
	Q, A
}
